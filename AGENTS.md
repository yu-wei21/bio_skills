# 生物信息分析脚本集合：协作规则

## 项目定位

这是个人维护的生物信息分析脚本整理集合，用于沉淀可复用、可审计的分析流程。脚本以 R 为主，覆盖单细胞 RNA-seq 预处理、注释、伪 bulk、差异表达、通路富集、可视化及格式转换。

每次开始工作，先阅读本文件和 [SCRIPT_INDEX.md](SCRIPT_INDEX.md)，再按索引定位最接近的脚本；不要重复创建已有能力的脚本。

## 修改前的最小检查

1. 先读取目标脚本和其上下游脚本，确认输入、输出、物种、基因 ID 与分析层级是否匹配。
2. 不修改原始数据、既有结果、队列定义、模型公式、协变量、随机种子或输出命名，除非用户明确授权。
3. 新增或修改涉及 R/Python 包的代码前，查阅该包与当前版本对应的官方教程、reference manual 或 vignette；采用当前推荐 API。不能核实时，要明确标注为未验证，不能猜测函数行为。
4. 仅做完成请求所需的最小改动；不要为局部问题重构无关脚本。

## 每个可执行脚本的必需头注释

在 shebang 之后、载入包之前，使用注释清楚写出：

1. **输入**：每个输入文件/目录、支持格式、必需列或对象结构、关键 ID/物种要求。
2. **输出**：输出目录、文件名规则，以及图形文件（PNG/PDF）如适用。
3. **示例命令**：可复制的 `Rscript <script> ...` 命令。
4. **注意事项**：仅在确有关键影响时增加，例如原始 count 要求、批次/样本级分析单位、内存消耗、包版本兼容性、硬编码设置或外部依赖；没有特殊注意时不要添加空泛的“注意事项”。

新增脚本优先接受命令行参数，默认结果写入 `output/<script_name>/`。只有项目特异性图形脚本可使用硬编码配置；这类脚本必须在头注释中明确标为“项目特异”，列出需要替换的配置位置和外部依赖。

## 分析与验证要求

- 计数型差异表达使用未归一化的非负整数 counts；单细胞比较需先明确是逐细胞分析还是按样本 pseudobulk。
- 对 join、过滤、聚合和去重，报告或检查输入/输出行（或细胞、样本）数与未匹配情况。
- 生成图形时，保存可复用的 PDF 和高分辨率 PNG（如脚本场景适用），并在 `SCRIPT_INDEX.md` 记录产物类型。
- 修改后至少做静态检查（参数、文件存在性、函数签名与依赖）；如具备最小测试数据，则运行最小示例。只有实际执行过，才能声称“已验证”。
- 发现逻辑、统计或 API 问题时，先复现并说明影响，再做最小修复；不得静默跳过样本、缺失值或异常值。

## 索引维护与每日扫描

- 新增、删除或实质修改脚本后，同步更新 `SCRIPT_INDEX.md`：分类、用途、输入、主输出、图形产物、关键依赖与验证状态。
- 每日 05:00 的应用内自动任务负责全仓扫描：更新索引，并按“分析必要性 × 通用性”最多提出 2 个候选包/脚本。候选流程必须先依据官方教程搭建最小脚本，保留与否由用户决定。
- 自动任务不得修改既有脚本的核心分析逻辑，除非问题可复现且修改范围明确。
